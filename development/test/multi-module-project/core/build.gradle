dependencies {
    compile 'com.google.guava:guava:31.1-jre'
    runtime 'ch.qos.logback:logback-classic:1.4.5'
}

task processData << {
    println 'Processing data in core module'
}

sourceSets {
    main {
        output.classesDir
    }
}
